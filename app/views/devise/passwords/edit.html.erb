<h2><%= t('devise.passwords.change_password_title').html_safe %></h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
  <%= devise_error_messages! %>
  <%= f.hidden_field :reset_password_token %>

  <div class="field">
    <%= f.label :password, t('devise.passwords.new_password_label').html_safe %><br />
    <% if @minimum_password_length %>
      <em>(<%= t('devise.passwords.minimum_length', minimum_length: @minimum_password_length).html_safe %>)</em><br />
    <% end %>
    <%= f.password_field :password, autofocus: true, autocomplete: "new-password" %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation, t('devise.passwords.confirm_password_label').html_safe %><br />
    <%= f.password_field :password_confirmation, autocomplete: "off" %>
  </div>

  <div class="actions">
    <%= f.submit t('devise.passwords.edit_submit_label').html_safe %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
